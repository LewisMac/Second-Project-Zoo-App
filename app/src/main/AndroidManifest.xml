<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.zooapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Zoo Tycoon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".MainApplication">

        <activity
            android:name=".MainActivity"
            android:label="Zoo Tycoon">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER"/>
                <action android:name="android.intent.action.MAIN"/>
            </intent-filter>
        </activity>

        <activity android:name=".AddAnimal">

        </activity>

        <activity android:name=".AddEnclosure">

        </activity>

        <activity android:name=".AddAnimalToEnclosure">

        </activity>
        <activity android:name=".EnclosureDisplay">

        </activity>

    </application>

</manifest>
